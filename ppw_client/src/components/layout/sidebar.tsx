'use client';
import Link from 'next/link'; import { usePathname } from 'next/navigation';
const links = [{ href: '/reservas', label: 'Reservas' }, { href: '/usuarios', label: 'Usuários' }, { href: '/espacos', label: 'Espaços' }];
export function Sidebar() { const pathname = usePathname(); return <header className="border-b border-slate-200 bg-white md:min-h-screen md:w-56 md:border-b-0 md:border-r"><div className="p-5"><Link href="/" className="text-xl font-bold text-teal-700">Agenda Coworking</Link><p className="mt-1 text-sm text-slate-500">Agendamento simples</p></div><nav className="flex gap-1 overflow-x-auto px-3 pb-3 md:block">{links.map((link) => <Link key={link.href} href={link.href} className={`block whitespace-nowrap rounded-lg px-3 py-2 text-sm font-medium ${pathname === link.href ? 'bg-teal-50 text-teal-800' : 'text-slate-600 hover:bg-slate-100'}`}>{link.label}</Link>)}</nav></header>; }
